/* cd CCSU_CourseWork/CS-355-Sys */
#include <stdio.h>
int main(void){
    double x;
    /*
     * &: address of _
    */
    printf("Enter a character:\n");
    scanf("%lf",&x);
    printf("The value of x is %lf \n",x);
    /*
     * %d for integers 
     * %f for floats
     * %c for characters
    */
}